{
    "name": "profiler",
    "version": "1.0.0",
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev --local --live-reload",
        "start": "wrangler dev"
    },
    "devDependencies": {
        "wrangler": "^4.20.0"
    },
    "dependencies": {
        "luxon": "^3.6.1"
    }
}
